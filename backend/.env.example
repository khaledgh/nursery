# App
APP_ENV=development            # development | production
APP_PORT=8080
APP_URL=http://localhost:8080
DEFAULT_LOCALE=en
# Comma-separated list of allowed browser origins (admin panel, etc.)
CORS_ORIGINS=http://localhost:5173

# Database
DB_HOST=localhost
DB_PORT=3306
DB_NAME=nursery
DB_USER=nursery
DB_PASSWORD=change-me

# Auth — generate with: openssl rand -base64 48  (min 32 chars enforced)
JWT_ACCESS_SECRET=change-me-to-a-long-random-string-please
JWT_REFRESH_SECRET=change-me-to-a-different-long-random-string
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=720h

# Initial admin (used by `make seed` only when the users table is empty)
SEED_ADMIN_EMAIL=admin@sunnystars.app
SEED_ADMIN_PASSWORD=

# Storage  (local | s3) — also overridable from admin settings
STORAGE_DRIVER=local
LOCAL_UPLOAD_DIR=./uploads
S3_BUCKET=
S3_REGION=
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_ENDPOINT=

# OneSignal
ONESIGNAL_APP_ID=
ONESIGNAL_API_KEY=

# Swish
SWISH_MERCHANT_ID=
SWISH_CERT_PATH=
SWISH_CALLBACK_URL=

# Integrations
WEATHER_API_KEY=
